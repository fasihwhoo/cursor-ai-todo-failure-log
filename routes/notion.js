const express = require('express');
const router = express.Router();

// This file is prepared for future endpoints if needed
// Currently, Notion sync is handled by the cron job

module.exports = router;
